# 🎯 秋招工作台生成器 · 使用提示词

> 把下面这段提示词 + 你的信息发给任意 AI（WorkBuddy / Claude / ChatGPT / DeepSeek / 豆包等），让它为你生成一个专属的秋招工作台 HTML 文件（双击浏览器打开即可用，数据存本地浏览器）。

## 使用步骤

1. 复制下面的【提示词模板】
2. 把【方括号】里的内容替换成你自己的信息
3. 发给 AI，得到 HTML 文件后保存为 `秋招工作台.html`，双击打开
4. 打开后点「➕ 添加单位」录入公司，点「💾 保存数据」定期备份

---

## 提示词模板（复制这段）

```
【任务】
帮我生成一个「秋招工作台」单文件 HTML（本地双击浏览器打开就能用，不需要服务器）。

【我的信息】
- 学校/专业：【如：XX大学 · XX专业】
- 目标岗位：【如：嵌入式软件工程师】
- 意向城市：【如：成都、重庆】
- 求职届次：【如：2027届】
- 我的项目/亮点：【简要描述 1-3 个项目或技能，用于生成时间线和打卡任务】

【工作台必须包含以下功能】
1. 投递追踪 Tab：
   - 顶部两张链接卡片：a「购入的秋招信息表」（放使用者买的秋招汇总表链接）b「我的信息源」（放使用者自己的信息源链接——招聘网站/公众号/内推群等；若使用者没提供，请你根据他的简历和意向岗位上网检索合适的岗位信息填入）
   - 表格列 = 序号/单位名称/性质/城市/工作地点/秋招时间/网申链接/笔试/薪资/投递状态/备注/简历文件/操作
   - 投递状态 9 种：未投递/已投递/测评中/面试中/已录用/已拒绝/未开启/链接失效/岗位不符
   - 支持：按城市筛选、按性质筛选、按状态筛选、关键词搜索、分页
   - 状态下拉直接修改并保存到 localStorage；链接可点击跳转 + ✏️ 可编辑
   - 顶部统计卡片：目标单位总数/已投递/面试测评中/已录用/未开启失效不符/意向城市数量
   - 公司数据初始为空（使用者自己添加或导入）
2. 秋招时间线 Tab：顶部横幅 + 6 个阶段卡片（提前批/正式批网申高峰/笔试密集期/面试高峰/签约谈薪/春招补录），每个阶段有时间段 + 通用任务提示（✏️ 占位，由使用者按自己行业填写），卡片左边框绿/红/蓝交替
3. 备战中心 Tab：
   - 多张链接卡片（占位）：八股资料、简历模板、求职工具、面试资料——链接由使用者自己提供
   - 面试记录功能：表格记录每次面试（公司/岗位/日期/轮次/结果/复盘备注），可添加/删除，存 localStorage
   - 每日打卡：顶部今日进度圆环（SVG 环形 + 百分比 + 鼓励语）；每天一行卡片（默认无预置任务，使用者点「+ 添加自定义任务」创建，可勾选、每个任务独立备注输入框、完成变绿+删除线）；周 Tab（上周/本周/下周/下下周）
4. 岗位匹配 Tab：10 项能力打分器（能力项按使用者目标岗位设定，使用者可在 HTML 中修改），每项三档（满足2分/部分1分/不满足0分），实时算匹配度百分比 + 等级 + 短板清单 + 保存历史可加载
5. 数据管理：数据存 localStorage；右上角按钮「💾 保存数据」（导出 JSON 备份）/「📂 打开数据」（导入恢复）/「🔄 重置全部」；首次打开自动从备份合并

【技术约束】
- 必须是单 HTML 文件，无任何外部依赖（不要 CDN 引用）
- JS 全部用单引号字符串 + 加号拼接生成 HTML，禁止跨行字符串（避免兼容问题）
- 样式：深灰渐变圆角 header（居中卡片式，带阴影）、pill 风格 Tab（等宽均分）、卡片化布局、浅灰背景
- 公司数据初始为空（用户自己点「➕ 添加单位」录入）
- 代码要能通过 JS 语法检查，生成后自己检查一遍

【输出】
直接输出完整 HTML 代码。
```

---

## 进阶：发给 AI 时的附加说明（可选）

如果 AI 生成的页面有细节不对，可以追加：

- 「投递状态选择框选项顺序：未投递/已投递/测评中/面试中/已录用/已拒绝/未开启/链接失效/岗位不符」
- 「备注列太长会撑高行，备注内容多时默认截断显示，点击展开」
- 「深灰渐变 header 用 #2d3436 → #636e72，圆角 16px」
- 「Tab 四个按钮 flex:1 等宽均分」

---

## 提示词设计思路（为什么这么写）

- **功能清单写死**：把你要的功能逐条列清楚，AI 不会漏
- **技术约束写死**：单文件、无依赖、单引号字符串、样式规范——保证生成结果稳定可用
- **个性化输入前置**：学校/岗位/城市/项目放在开头，AI 会用它填充时间线、打卡任务、岗位匹配项
- **数据从零开始**：公司表初始为空，由使用者自己录入——这样换任何人都能用，不绑死你的数据
